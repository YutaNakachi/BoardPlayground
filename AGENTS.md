# ボドパッ！ — Agent 方針

オリジナルボードゲームの Web 公開・プレイ用リポジトリ。

## 基本方針

- **日本語**で応答
- ゲームの **系統**（下記）に沿って掲載・実装する
- **商標・商品名**を公開文・UI に出さない（トリビュート・フィクション共通）
- 新ゲーム追加の手順: `lib/games.ts` → `games/{slug}/rules.md` → `components/play/` → `lib/play-registry.ts`（詳細は「新規ゲーム追加」）

## ゲームの系統

一覧・ルールページでプレイヤーに見えるラベル（`lib/games.ts` の `origin` / `ORIGIN_LABEL`）。

| `origin` | 表示 | 意味 |
|---|---|---|
| `original` | オリジナル | 参照元の特定商品・作品がない独自ゲーム |
| `classic` | クラシック | 商標を使わない伝統的・公有域のボードゲーム |
| `tribute` | トリビュート | 市販ボードゲームのルールに**できるだけ近い**、ボドパッ！独自の**非公式**プレイ版 |
| `fiction` | フィクション | 漫画・アニメ等の**作品に登場する**ゲームのプレイ版（不足ルールはサイト版で補完） |

**トリビュート**

- 体験目標は公式ルールに近い再現（オーナー判断で掲載）。
- ルールブックの**文章コピペはしない**（説明は自前の `rules.md`）。
- プレイ画面・ルールで**非公式**であることが分かる表示を入れる。
- 収益化の基本はサイト本体に結びつけない。購入リンクを貼る場合は**アフィリエイト**とし、**関係の開示**を行う（公式・権利者提供と誤認されない文言）。
- 参照した市販ゲーム名は**公開説明に出さない**（内部メモ・issue で可）。
- **実装着手前**にオーナーの OK（チャットでの口頭・明示返信で可）。

**フィクション**

- 作品内でルールが成立している部分は**ほぼそのまま**再現する。
- 作品にない部分はオーナーまたは Agent が補完し、`rules.md` で**作品由来**と**ボドパッ！版の補完**を分けて書く。
- 作品名・キャラクター・セリフ・画像の利用は別途オーナー指示に従う（無指示なら商品名と同様、公開文に作品名を出さない運用を基本）。

**権利・再現について（Agent 向け整理・法律相談ではない）**

- 「ルールのアイデア」だけと「ルールブックの文章・用語の表現」は別問題。後者の丸写しは避ける。
- 同じ手順・勝利条件の実装でも、**商標・誤認（公式と思わせる見せ方）**や**不正競争**の論点は残る。
- ゲームの一部に**特許**がある場合もある（全体ではなくメカニクスの一部）。トリビュート掲載はオーナー GO を前提にする。

## 新規ゲーム追加

### 着手前に必須

- **ゲーム名**（仮タイトル可）と **slug**
- **人数・勝利条件・手番の流れ**など、ルールとしての仕様
- **系統**（`original` / `classic` / `tribute` / `fiction`）
- **トリビュート**は上記のとおり**オーナー OK 後**に Phase 1（実装）へ

### 後から決めてよい

- **プレイ時間**（`durationMinutes`）
- **難易度**（`complexity`）

### 掲載（`listed`）

- 実装開始時は **`listed: false`**（一覧に出さない）。
- 完成後、**オーナーが掲載を判断**して `listed: true` にする。

### ルール文（`rules.md`）

- 仕様が決まったら Agent が `rules.md` を書き始める。
- 実装と並行してルールを調整する場合は、**都度 `rules.md` に反映**する。

### オーナーの関わり方

- **基本（A）**: 企画・ルール草案はオーナー → Agent は実装。
- **必要時（B）**: 企画も Agent が行うが、**実装着手前にオーナーの承認**（チャットで OK / 進めて 等）。承認前は企画の提示のみ。

### 1 ゲームあたりの PR 範囲（ゲーム追加 Agent）

| 含める | 含めない |
|---|---|
| `lib/games.ts`（`listed: false`） | ユニットテスト（**ゲームロジック検証 Agent・別 PR**） |
| `games/{slug}/rules.md` | カードアート（**掲載決定後**など別対応） |
| プレイ実装・`lib/play-registry.ts` | 文章推敲の専門 Pass（**文章・文言検証 Agent・別 PR**） |
| `npm run catalog:doc` で更新した `docs/game-catalog.md` | |
| PR 本文に**手動テスト手順** | |

### Cloud Agent：ゲーム追加で守ること

1. **ゲーム名・slug が明示されていないときは実装しない**（`docs/agent-prompts.md` の `{ゲーム名}` だけのプロンプトでは着手しない）。
2. **仕様の合意前に** `lib/games.ts` やプレイ画面を書かない。
3. **仕様が曖昧なときは勝手にゲームを企画しない**（オーナーから企画依頼があったときは例外）。
4. 着手前に **やること・やらないこと・PR の範囲** を短く確認する。

Cursor ルール: `.cursor/rules/game-addition.mdc`

## ルール仕様の整理

1. 人数・時間・勝利条件を明確化
2. 手番の例外・タイブレークを列挙
3. `rules.md` とプレイ実装の整合を保つ

## プレイ実装

- 最初は **ローカルプレイ**（同画面交代）
- クライアントコンポーネントは `"use client"`
- ゲームごとに `components/play/{Name}Game.tsx`

### プレイ画面のレイアウト

盤面の位置が上下にズレないよう、次の順序と表示場所を守る。

1. **ResultPanel**（終了時）→ **TurnBanner**（手番中）→ **盤上ツールバー**（任意・常時）→ **盤面** → **盤下アクション** → **メッセージ窓**（任意・将来）

**メッセージ**

- プレイ中に出たり消えたりする短いメッセージ（パス通知、非法手、フェーズ案内など）は、基本 **TurnBanner 内の1行**（手番ラベルの下。`stats` / `action` / `notice` を ` · ` で連結）に表示する。
- **盤上・TurnBanner 外**に、出たり消えたりするテキストメッセージを置かない。
- 複数行・履歴・選択肢説明など複雑なメッセージが必要なゲームは、盤の一番下にメッセージ窓（または固定行）を用意する。既存ゲームでは不要なら作らない。

**ボタン**

- プレイ中に出たり消えたりする操作ボタン（サイコロ、スワップ、パスなど）は**盤の下**に置く。
- **常時表示**の UI（凡例、リセット、盤サイズ表示など）は盤の上（TurnBanner の直下）または盤内に置いてよい。

**TurnBanner**

- 手番ラベル下のサブタイトル行は1行にまとめ、出し入れで盤が動かないよう空でも高さを確保する（`components/play/shared/TurnBanner.tsx`）。
- `stats` は常時表示の進行情報、`action` は次にやること、`notice` は状況通知（パス・非法手など）。複数あるときは ` · ` で連結。
- 手番ラベル行だけは常に表示。終了後は TurnBanner ごと非表示でよい。

## デザイン

- ダークテーマ、accent は indigo 系（`tailwind.config.ts`）
- モバイルでもプレイ可能なレイアウト

## Agent の分け方

初回プロンプトのコピペ用: `docs/agent-prompts.md`

- **サイト改善**（Cloud Agent 向け）: 一覧・ルールページ・ヘッダー/フッター、about、デプロイ関連、共通 UI。新ゲームは追加しない
- **Cloud Agent 環境**: `.cursor/environment.json`・Dockerfile・`cloud-agent-*.sh` の変更。マージ前に Rebuild 成功と `npm run env:check` を確認。ルール: `.cursor/rules/cloud-agent-env.mdc`
- **ゲーム追加**: 1ゲームにつき 1 Agent。上記「新規ゲーム追加」に従う。`.cursor/rules/game-addition.mdc`
- **ゲームロジック検証**: ゲーム変更の前後で起動（常設ではない）。`rules.md` と実装の一致、境界ケース、`lib/play/*.ts` のテスト。サイト改善 PR には触れない
- **文章・文言検証**: サイト横断の日本語・説明の正確さ（常設ではない）。ルール文・一覧説明・UI 文言のわかりやすさと表記統一。ゲームロジックの実装変更はしない
- **オンライン対戦**: 1機能 or 1ゲームにつき 1 Agent（常設ではない）。部屋・Realtime・API・既存ゲームのオンライン化。将来構想は `docs/future-online-lobby.md`
- **CPU 対戦**: 1ゲームにつき 1 Agent（常設ではない）。`lib/play/{slug}/` にロジックがある前提で `ai.ts` を追加し、`lib/games.ts` の `cpu: true` を更新。手札非公開ゲームはオンライン部屋対応後
- ゲーム固有ルールまで一般化しない。同じ処理が3本目で必要になったら共通化してよい

### ゲームロジック検証 Agent

- **担当**: `games/*/rules.md`（仕様の読み取り）、`lib/play/`、`components/play/{Name}Game.tsx`、`lib/play/check-engines.ts`、`lib/online/moves.ts`
- **やる**: 勝利条件・手番交代・パス・同点など仕様どおり動くかの確認、非法手の拒否、ロジックのユニットテスト、変更ゲームの手動テスト手順を PR に記載
- **やらない**: 新ゲーム企画、文章の推敲・表記統一、サイト一覧・共通 UI、CPU 実装
- ロジックがコンポーネント内だけにあるゲームは、検証可能なら先に `lib/play/` へ抽出する
- ルール Cursor ルール: `.cursor/rules/game-logic-verification.mdc`

### 文章・文言検証 Agent

- **担当**: `games/*/rules.md`（文章）、`lib/games.ts`（`title` / `description` / `rulesSummary`）、`app/` のページ文、`components/` の表示文言、`app/layout.tsx` のメタデータ
- **やる**: 日本語の正確さ（誤字・文法・用語）、説明のわかりやすさ、同一概念の表記統一、`rules.md` と一覧・プレイ画面の説明が矛盾しないかの確認。トリビュート・フィクションでは**商品名・作品名・公式誤認**の表現チェック
- **やらない**: ルール仕様の変更（実装と食い違う記述の**指摘**はする。直すのはゲーム追加 or ロジック検証と連携）、レイアウト変更、新ゲーム追加、ロジック修正
- ルール Cursor ルール: `.cursor/rules/copy-verification.mdc`

### オンライン対戦 Agent

- **担当**: `lib/online/`、`hooks/useOnlineRoom.ts`、`app/api/rooms/`、部屋関連マイグレーション、`OnlineSetupPanel`、各ゲームのオンライン統合、`docs/future-online-lobby.md`
- **やる**: 部屋作成・参加・同期の改善、既存ゲームのオンライン化（`lib/online/moves.ts` + `ONLINE_GAME_SLUGS`）、再接続・再戦などロビー機能（フェーズごとに小さく PR）
- **やらない**: 新ゲーム追加、ローカル専用ルール変更、サイト横断 UI、CPU 実装、手札非公開ゲームのオンライン
- ルール Cursor ルール: `.cursor/rules/online-play.mdc`

### CPU 対戦 Agent

- **担当**: `lib/play/{slug}/ai.ts`（新規）、対象ゲームのプレイ画面、`lib/games.ts` の `cpu` フラグ
- **やる**: 合法手生成を使った弱・普通・強の CPU 手番、UI は CPU 手番時に `chooseMove` を呼ぶだけに留める
- **やらない**: 複数ゲームを1 PR でまとめる、手札非公開ゲームの CPU（スター・トレード等）
- CPU インターフェース（`getLegalMoves` / `chooseMove` / `difficulty`）は3本目で `lib/play/cpu/` などへ共通化を検討

### 推奨フロー

1. ゲーム追加 Agent → ルール・実装・PR（`listed: false`、`game-catalog.md` 更新）
2. **別 PR**: ゲームロジック検証 Agent → 境界ケース確認・ユニットテスト
3. **別 PR**: 文章・文言検証 Agent → ルール文・一覧・UI 文言
4. オンライン対戦 Agent → 2人・盤面公開ゲームのみ別 PR（既存4本は対応済み。新ゲーム or 部屋機能改善）
5. CPU 対戦 Agent → 必要なゲームだけ別 PR
6. 掲載判断後: `listed: true`、必要ならカードアート（サイト改善 or 別 PR）

サイト改善 PR のマージ前に、必要なら文章・文言検証 Agent を回す。
